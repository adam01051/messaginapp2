{
	"name": "3rd_messagin_application",
	"version": "1.0.0",
	"description": "//  building this  for  capstone  device  will  write  desc  later",
	"main": "index.js",
	"scripts": {
		"build": "npm ci --include=dev --prefix backend && npm ci --include=dev --prefix frontend && npm run build --prefix backend && npm run build --prefix frontend",
		"release": "npm run prisma:migrate:deploy --prefix backend",
		"check": "npm run prisma:validate --prefix backend && npm run build --prefix backend && npm test --prefix backend && npm run lint --prefix frontend && npm run build --prefix frontend",
		"dev:backend": "npm run dev --prefix backend",
		"dev:frontend": "npm run dev --prefix frontend",
		"start:backend": "npm run start --prefix backend",
		"start:frontend": "npm run start --prefix frontend"
	},
	"engines": {
		"node": ">=20.19.0"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/adam01051/3rd_messagin_application.git"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"bugs": {
		"url": "https://github.com/adam01051/3rd_messagin_application/issues"
	},
	"homepage": "https://github.com/adam01051/3rd_messagin_application#readme"
}
